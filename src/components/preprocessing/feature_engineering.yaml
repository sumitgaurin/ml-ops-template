# <component>
name: paynet_feature_engineering
display_name: Paynet - Feature Engineering Component
description: Takes the input dataset and performs cleansing and transformation for the experiment
version: 7
type: command
inputs:
  dataset_path:
    type: uri_folder
    description: Path to the input raw dataset
outputs:
  transformed_data_path:
    type: uri_folder
    description: Path to the transformed dataset after feature engineering
code: .
command: >
  python feature_engineering.py
  --dataset_path ${{inputs.dataset_path}}
  --output_path ${{outputs.transformed_data_path}}
environment: azureml:sklearn-dev310@latest
